node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# Cloudflare
.dev.vars
.dev.vars.*
!.dev.vars.example
# `wrangler types` の生成物。wrangler.toml の main がビルド済みかどうかで
# 内容が変わるため、コミットせず prepare / build で毎回生成する。
worker-configuration.d.ts

# Claude Code のワークツリーとローカル設定。
# .claude/agents/（Evaluator など）と .claude/settings.json は共有するのでコミットする。
/.claude/worktrees/
/.claude/settings.local.json

# エージェントの作業メモ（AGENTS.md「作業の手順」）
/TASKS.md

# PR 用のキャプチャ（pnpm run screens）。コミットせず、pnpm run screens:pr が
# 画像置き場の screenshots ブランチへ上げる（docs/testing.md 第5章）
/docs/screenshots/
